@model IEnumerable<NewGP.Models.findpeople>


@{
    ViewBag.Title = "search";
}
<br />
<h2 style="color: black; font-family: 'Poppins'; font-size: 100px; text-align-last: center;">Search</h2>

@using (Html.BeginForm("search", "Findpeople", FormMethod.Get))
{
    @Html.TextBox("searching")
    <input type="submit" value="search" />
}

<table class="table" style="color:black">
    <thead>
        <tr>
            <th>First name</th>
            <th>Last name</th>
            <th>Age</th>
            <th>City</th>
            <th>Date of missing</th>
            <th>Description Of Clothes</th>
            <th>Gender</th>
        </tr>
    </thead>
    <tbody>
        @*@if (Model.Count == 0 )
            {
                <tr>
                    <td colspan="7" style="color:red">
                        No Match
                    </td>
                </tr>
            }
            else
            { *@

        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Firstname</td>
                <td>@item.Lastname</td>
                <td>@item.Age</td>
                <td>@item.city</td>
                <td>@item.Dateoffound<td>
                <td>@item.DescripClothes</td>
                <td>@item.Ismale</td>
            </tr>
        }
        
    </tbody>
</table>

