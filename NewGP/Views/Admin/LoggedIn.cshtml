@model NewGP.Models.Admin

@{
    ViewBag.Title = "LoggedIn";
}

<h4>Hello @Session["UserName".ToString()]</h4>

<p>@Html.ActionLink("List Users", "ListUsers")</p>
<p>@Html.ActionLink("List Admins", "Listadmin")</p>

<br />
@using (Html.BeginForm("SearchUser", "Admin", FormMethod.Post))

{
    @Html.TextBox("search")
    <input type="submit" value="search" />
}
<div class="col-2">
    <button class="brn btn-warning">@Html.ActionLink("Back to list", "Index", "Admin")</button>
</div>